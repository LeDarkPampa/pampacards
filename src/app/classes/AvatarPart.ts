export class AvatarPart {
  head: string;
  hat: string;
  body: string;
  back: string;
  add: string;

  constructor(head: string, hat: string, body: string, back: string, add: string) {
    this.head = head;
    this.hat = hat;
    this.body = body;
    this.back = back;
    this.add = add;
  }
}

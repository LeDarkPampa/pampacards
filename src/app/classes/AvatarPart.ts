export class AvatarPart {
  head: string;
  hat: string;
  body: string;
  back: string;
  add: string;
  front: string;

  constructor(head: string, hat: string, body: string, back: string, add: string, front: string) {
    this.head = head;
    this.hat = hat;
    this.body = body;
    this.back = back;
    this.add = add;
    this.front = front;
  }
}

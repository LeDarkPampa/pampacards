export class AvatarPart {
  head: string;
  hat: string;
  body: string;
  back: string;

  constructor(head: string, hat: string, body: string, back: string) {
    this.head = head;
    this.hat = hat;
    this.body = body;
    this.back = back;
  }
}

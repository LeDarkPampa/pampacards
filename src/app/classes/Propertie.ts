export class Propertie {
  id: number;
  code: string;
  value: boolean;

  constructor(id: number, code: string, value: boolean) {
    this.id = id;
    this.code = code;
    this.value = value;
  }
}
